import wollok.game.*

import tablero.*
/*
program PepitaGame {
	game.title("Pepita")
	game.height(10)
	game.width(10)

	// add assets in asset folder, for example, for the background
  // game.boardGround("fondo2.jpg")

	//

	game.showAttributes(pepita) //Debug

	game.start()
}
*/

program tab{
	tablero.inicializar()
	game.start()
	keyboard.q().onPressDo({console.println("Q presionada!") flecha.desplazarse()})
	keyboard.r().onPressDo({flecha.position(game.at(0,24))})
}